# Development Dockerfile for Web
FROM node:22-alpine

WORKDIR /app

# No need to copy or install - we'll use mounted volumes

# Set environment
ENV NODE_ENV=development
ENV PORT=3000

EXPOSE 3000

WORKDIR /app/apps/web

# Use development server
CMD ["npm", "run", "dev"]